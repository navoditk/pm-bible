# Use Case — Benchmark-Relative Risk

Given portfolio weights, benchmark weights, and covariance:
1. calculate active weights
2. tracking error
3. largest active positions
4. active risk contribution
5. identify what constraint would most directly reduce TE
